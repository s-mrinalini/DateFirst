# DateFirst backend — direct dependencies only.
#
# Anything not listed here is resolved transitively by pip. Pinning transitive
# packages caused a Render build failure (google-api-core[grpc]==2.29.0 vs
# grpcio-status==1.71.2) and is also why this file used to be 137 lines.

# --- Web framework -----------------------------------------------------------
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart==0.0.22       # FastAPI requires this for UploadFile / Form

# --- Validation & config -----------------------------------------------------
pydantic==2.12.5
email-validator==2.3.0          # required by pydantic.EmailStr
python-dotenv==1.2.1

# --- Database driver ---------------------------------------------------------
motor==3.3.1
# Transitive pin: motor 3.3.1 imports pymongo's `_QUERY_OPTIONS`, which was
# removed in pymongo 4.6. Drop this once we bump motor to 3.5+.
pymongo<4.6

# --- Auth & crypto -----------------------------------------------------------
PyJWT==2.11.0
argon2-cffi==25.1.0

# --- Real-time ---------------------------------------------------------------
python-socketio==5.16.1

# --- File storage & HTTP -----------------------------------------------------
boto3==1.42.41
aiofiles==25.1.0
httpx==0.28.1

# --- Image processing --------------------------------------------------------
Pillow==12.1.0
# python-magic needs libmagic at the OS level. On Render add an aptfile with
# `libmagic1`, or install via the build command. image_processing.py degrades
# gracefully if libmagic is missing (Pillow-only sniff).
python-magic==0.4.27

# --- External integrations ---------------------------------------------------
resend==2.5.1
twilio==9.4.6

# --- Content moderation ------------------------------------------------------
better-profanity==0.7.0
